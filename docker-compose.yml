services:
    db:
      image: postgres:16
      container_name: teste-db
      restart: always
      environment:
        POSTGRES_USER: root
        POSTGRES_PASSWORD: root
        POSTGRES_DB: express_class
      ports:
        - "5433:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data

volumes:
    postgres_data: